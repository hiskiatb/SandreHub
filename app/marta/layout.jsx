export const metadata = {
  title: "MartaHub — Login",
  description: "MartaHub Marketing Execution Platform — akses untuk tim IOH Sumatera.",
};

export default function MartaAuthLayout({ children }) {
  return children;
}
